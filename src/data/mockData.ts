import { SurveyRecord } from '../types/survey';

/**
 * Initial survey records list is clean and empty.
 * Users can upload their own Excel spreadsheet or create records.
 */
export const INITIAL_RECORDS: SurveyRecord[] = [];
